<Window x:Class="ДорожноеПриложение.PaymentPeriodWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ДорожноеПриложение"
        mc:Ignorable="d"
        Title="Выбор периода" Height="300" Width="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="C этой даты" HorizontalAlignment="Right" />
        <TextBlock Text="C этой даты" HorizontalAlignment="Right" Grid.Row="1"/>

        <DatePicker x:Name="sData" Grid.Column="1"/>
        <DatePicker x:Name="poData" Grid.Column="1" Grid.Row="1"/>

        <Button x:Name="Ok" Content="Ok" Click="Ok_Click" Margin="5" Grid.Row="2" Grid.Column="1"/>
    </Grid>
</Window>
